<?php
// config.local.example.php — Template mẫu cấu hình database
// [SEC-06] Sao chép file này thành config.local.php và điền thông tin thật
// File này AN TOÀN để commit vào git (không chứa credentials thật)

// Bước 1: cp config.local.example.php config.local.php
// Bước 2: Chỉnh sửa config.local.php với thông tin DB của bạn
// Bước 3: KHÔNG commit config.local.php vào git

// define('DB_HOST', 'localhost');
// define('DB_USER', 'warehouse_user');  // User có quyền SELECT/INSERT/UPDATE/DELETE only
// define('DB_PASS', 'your-strong-password-here');
// define('DB_NAME', 'warehouse_manager');
